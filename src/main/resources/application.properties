## From email address
#app.email.from=shareview.ct.ws
#app.email.sender-name=Shareview
# ============================================
# SENDGRID CONFIG
# ============================================
sendgrid.api.key=${SENDGRID_API_KEY}
sendgrid.from.email=shareview682@gmail.com
sendgrid.from.name=Shareview

# ============================================
# EMAIL CONFIG - GMAIL SMTP
# ============================================
app.email.from=shareview682@gmail.com
app.email.sender-name=Shareview

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=shareview682@gmail.com
spring.mail.password=arhv nidh woqn hyvc

spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# ============================================
# DATABASE & SERVER CONFIG
# ============================================
server.port=${PORT:8080}
spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}
spring.jpa.hibernate.ddl-auto=update

## ============================================
## LOGGING - INCREASE FOR DEBUGGING
## ============================================
#logging.level.Shareview.service.MailjetEmailService=DEBUG
#logging.level.Shareview.service.EmailService=DEBUG
#logging.level.com.sendgrid=DEBUG

# ============================================
# APPLICATION CONFIG
# ============================================
app.otp.expiration.minutes=5
app.max.saved.posts=50

# File upload settings
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=100MB
spring.servlet.multipart.max-request-size=100MB
spring.servlet.multipart.file-size-threshold=2MB

# Static resource serving
spring.web.resources.static-locations=classpath:/META-INF/resources/,classpath:/resources/,classpath:/static/,classpath:/public/,file:./uploads/

# Jackson configuration
spring.jackson.serialization.FAIL_ON_EMPTY_BEANS=false

# Cloudinary configuration
cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME}
cloudinary.api-key=${CLOUDINARY_API_KEY}
cloudinary.api-secret=${CLOUDINARY_API_SECRET}

# Actuator
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=when-authorized
management.health.db.enabled=true

## Disable Spring Mail auto-configuration since we're using SendGrid Web API
#spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.mail.MailSenderAutoConfiguration

#mailjet.api.key=${MAILJET_API_KEY}
#mailjet.from.email=${MAILJET_FROM_EMAIL}
#mailjet.from.name=${MAILJET_FROM_NAME}
#mailjet.secret.key=${MAILJET_SECRET_KEY}

#gemini.api.key=${GEMINI_API_KEY}
gemini.api.key=${gemini_api_key}
gemini.model=${gemini_model}